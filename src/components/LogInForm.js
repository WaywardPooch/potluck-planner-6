import { Form, FormGroup, Input } from 'reactstrap';

function LogInForm(props){
    return (
        <Form>
            <FormGroup>
                <Input type='submit'/>
            </FormGroup>
        </Form>
    );
}

export default LogInForm